import { RackBuilder } from './rack/RackBuilder';

/** Standalone shell — same behavior as before refactor: full page, localStorage, app header. */
export function App() {
  return <RackBuilder />;
}
